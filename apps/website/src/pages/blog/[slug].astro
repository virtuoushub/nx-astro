---
import Layout from '../../layouts/BlogPostLayout.astro';
import { BlogPost } from '@leosvel/blog';

export async function getStaticPaths() {
  const posts = await Astro.glob('../../blog-posts/*.md')
  // return posts.map((post) => ({
  //   params: { slug: post.frontmatter.slug },
  //   props: { post: post.frontmatter },
  // }));
  return [ {params: { blog: "creating-my-personal-website-with-astro-tailwindcss-and-nx"}}]
}

// const {
//   Content,
//   title,
//   description,
//   date,
//   heroImage,
//   heroImageAlt,
//   thumbnailImage,
//   thumbnailImageAlt,
//   canonicalUrl,
// } = Astro.props.frontmatter;
// console.error({canonicalUrl})
// const originalUrl = !canonicalUrl?.startsWith(Astro.site.href)
//   ? canonicalUrl
//   : undefined;
---

<>yolo</>